# 学科知识整合智能体 — 系统设计

## 1. 架构概览

```
┌─────────────────────────────────────────────────────┐
│                    前端 (React + TS)                  │
│  ┌──────────┬──────────┬──────────┬────────────────┐ │
│  │教材管理  │知识图谱  │智能问答  │ 竞学堂对抗     │ │
│  │(上传解析)│(ECharts) │(RAG QA)  │ (Arena)        │ │
│  └────┬─────┴────┬─────┴────┬─────┴───────┬────────┘ │
│       │          │          │             │           │
│  ─────┼──────────┼──────────┼─────────────┼──────────│
│       │    Zustand 状态管理  │             │           │
└───────┼──────────┼──────────┼─────────────┼──────────┘
        │  Axios / SSE / Fetch│             │
┌───────┼──────────┼──────────┼─────────────┼──────────┐
│       ▼          ▼          ▼             ▼           │
│  ┌─────────────────────────────────────────────────┐  │
│  │              FastAPI 后端                        │  │
│  │  ┌──────────┬──────────┬──────────┬──────────┐  │  │
│  │  │教材解析  │图谱服务  │ RAG服务  │竞学堂    │  │  │
│  │  │API       │API       │ API      │API       │  │  │
│  │  └────┬─────┴────┬─────┴────┬─────┴────┬─────┘  │  │
│  │       │          │          │          │         │  │
│  │  ┌────▼────┐┌────▼────┐┌───▼────┐┌───▼──────┐  │  │
│  │  │PyMuPDF  ││LLM服务  ││Embedding││LLM出题  │  │  │
│  │  │python-  ││(OpenAI) ││(BGE)   ││评判     │  │  │
│  │  │docx     ││         ││FAISS   ││嘲讽生成 │  │  │
│  │  └─────────┘└─────────┘└────────┘└──────────┘  │  │
│  └─────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────┘
```

## 2. 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| 前端框架 | React 18 + TypeScript | UI构建 |
| 样式 | Tailwind CSS | 快速样式开发 |
| 状态管理 | Zustand | 轻量级状态管理 |
| 图谱可视化 | ECharts | 知识图谱力导向图 |
| HTTP客户端 | Axios + Fetch SSE | API通信和流式输出 |
| 后端框架 | FastAPI | 高性能异步API |
| LLM | OpenAI-compatible API | 知识提取、问答、评判 |
| 嵌入模型 | sentence-transformers (BGE-small-zh) | 中文文本嵌入 |
| 向量库 | FAISS | 向量相似度检索 |
| 文档解析 | PyMuPDF + python-docx | PDF/DOCX解析 |

## 3. 核心数据模型

### 3.1 教材相关
```
TextbookMeta: {id, filename, format, chapter_count}
Chapter: {id, textbook_id, textbook_name, title, content, page_start, page_end}
KnowledgePoint: {id, chapter_id, textbook_id, name, description, type, page}
Chunk: {id, textbook_id, chapter_id, content, page, embedding}
```

### 3.2 图谱相关
```
GraphNode: {id, label, type, textbook_id, frequency, size}
GraphEdge: {source, target, relation, weight}
KnowledgeGraph: {nodes[], edges[]}
```

### 3.3 整合相关
```
IntegrationPair: {kp_a, kp_b, similarity, decision, reason, merged_content}
IntegrationResult: {pairs[], compression_ratio, original_count, final_count}
```

### 3.4 竞学堂相关
```
ArenaOpponent: {id, name, personality, style}
ArenaQuestion: {id, question, options[], correct_answer, explanation}
ArenaRound: {round_num, question, opponent_answers, user_answer, is_correct, feedback}
ArenaSession: {id, mode, rounds[], score, streak, weak_points[]}
```

## 4. API设计

### 教材管理

#### POST /api/textbooks/upload — 上传教材

```bash
curl -X POST http://localhost:8000/api/textbooks/upload \
  -F "file=@test.pdf"
```

响应示例：
```json
{
  "id": "tb_001",
  "filename": "test.pdf",
  "format": "pdf",
  "chapter_count": 5,
  "knowledge_points_count": 42,
  "chunks_count": 68
}
```

#### GET /api/textbooks/list — 列表

```bash
curl http://localhost:8000/api/textbooks/list
```

响应示例：
```json
[
  {
    "id": "tb_001",
    "filename": "test.pdf",
    "format": "pdf",
    "chapter_count": 5,
    "upload_time": "2026-05-10T10:30:00"
  }
]
```

#### DELETE /api/textbooks/{id} — 删除

```bash
curl -X DELETE http://localhost:8000/api/textbooks/tb_001
```

响应示例：
```json
{
  "message": "Textbook tb_001 deleted successfully",
  "deleted_chunks": 68,
  "deleted_knowledge_points": 42
}
```

### 知识图谱

#### GET /api/knowledge/graph — 获取图谱

```bash
curl http://localhost:8000/api/knowledge/graph
```

响应示例：
```json
{
  "nodes": [
    {
      "id": "kp_001",
      "label": "细胞膜",
      "type": "concept",
      "textbook_id": "tb_001",
      "frequency": 5,
      "size": 30,
      "chapter_title": "第一章 细胞结构",
      "description": "细胞膜是包围细胞质的薄膜，由磷脂双分子层和蛋白质组成"
    }
  ],
  "edges": [
    {
      "source": "kp_001",
      "target": "kp_002",
      "relation": "contains",
      "weight": 0.8
    }
  ]
}
```

#### GET /api/knowledge/points — 获取知识点列表

```bash
curl http://localhost:8000/api/knowledge/points
```

响应示例：
```json
[
  {
    "id": "kp_001",
    "name": "细胞膜",
    "type": "concept",
    "textbook_id": "tb_001",
    "chapter_id": "ch_001",
    "chapter_title": "第一章 细胞结构",
    "description": "细胞膜是包围细胞质的薄膜，由磷脂双分子层和蛋白质组成"
  }
]
```

#### POST /api/knowledge/integrate — 执行整合分析

```bash
curl -X POST http://localhost:8000/api/knowledge/integrate
```

响应示例：
```json
{
  "pairs": [
    {
      "kp_a": {
        "id": "kp_001",
        "name": "细胞膜",
        "description": "细胞膜是包围细胞质的薄膜，由磷脂双分子层和蛋白质组成",
        "textbook_id": "tb_001"
      },
      "kp_b": {
        "id": "kp_015",
        "name": "细胞质膜",
        "description": "细胞质膜又称细胞膜，是细胞与外界环境的边界",
        "textbook_id": "tb_002"
      },
      "similarity": 0.92,
      "decision": "merge",
      "reason": "两个知识点描述同一概念，前者更详细，后者提供了补充信息",
      "merged_content": "细胞膜（又称细胞质膜）是包围细胞质的薄膜，由磷脂双分子层和蛋白质组成，是细胞与外界环境的边界"
    }
  ],
  "stats": {
    "original_count": 42,
    "final_count": 34,
    "compression_ratio": 0.803,
    "merge_count": 5,
    "keep_count": 3,
    "remove_count": 0,
    "enrich_count": 0
  },
  "integrity_report": {
    "dependency_integrity": true,
    "coverage_report": {
      "概念": {"original": 20, "kept": 18, "ratio": 0.9},
      "定理": {"original": 8, "kept": 7, "ratio": 0.875},
      "方法": {"original": 10, "kept": 9, "ratio": 0.9},
      "现象": {"original": 4, "kept": 4, "ratio": 1.0}
    },
    "broken_chains": []
  }
}
```

#### POST /api/knowledge/integrate/apply — 应用整合方案

```bash
curl -X POST http://localhost:8000/api/knowledge/integrate/apply \
  -H "Content-Type: application/json" \
  -d '{
    "pairs": [
      {
        "kp_a_id": "kp_001",
        "kp_b_id": "kp_015",
        "decision": "merge",
        "merged_content": "细胞膜（又称细胞质膜）是包围细胞质的薄膜，由磷脂双分子层和蛋白质组成"
      }
    ]
  }'
```

响应示例：
```json
{
  "message": "Integration applied successfully",
  "updated_nodes": 2,
  "removed_nodes": 1,
  "updated_edges": 5
}
```

### 智能问答

#### POST /api/qa/ask — 普通问答

```bash
curl -X POST http://localhost:8000/api/qa/ask \
  -H "Content-Type: application/json" \
  -d '{"question": "什么是细胞膜？它的主要功能是什么？"}'
```

响应示例：
```json
{
  "answer": "细胞膜是包围细胞质的薄膜，由磷脂双分子层和蛋白质组成。它的主要功能包括：\n1. 作为细胞的边界，维持细胞形态\n2. 控制物质进出细胞（选择性透过）\n3. 进行细胞识别和信号传递\n4. 提供细胞内外环境的稳定环境",
  "references": [
    {
      "textbook": "test.pdf",
      "chapter": "第一章 细胞结构",
      "page": 15,
      "excerpt": "细胞膜是包围细胞质的薄膜，由磷脂双分子层和蛋白质组成。磷脂分子排列成双层，蛋白质镶嵌其中或贯穿膜层..."
    },
    {
      "textbook": "test.pdf",
      "chapter": "第二章 物质运输",
      "page": 32,
      "excerpt": "细胞膜的选择透过性是指细胞膜能让水分子自由通过，要选择吸收的离子和小分子也可以通过..."
    }
  ],
  "confidence": 0.92
}
```

#### POST /api/qa/ask/stream — SSE流式问答

```bash
curl -X POST http://localhost:8000/api/qa/ask/stream \
  -H "Content-Type: application/json" \
  -d '{"question": "什么是细胞膜？"}'
```

响应示例（SSE流）：
```
data: {"type": "token", "content": "细胞"}
data: {"type": "token", "content": "膜是"}
data: {"type": "token", "content": "包围细胞质的薄膜..."}
data: {"type": "reference", "reference": {"textbook": "test.pdf", "chapter": "第一章", "page": 15}}
data: {"type": "done"}
```

#### POST /api/qa/teacher-chat — 教师对话

```bash
curl -X POST http://localhost:8000/api/qa/teacher-chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "为什么要把细胞膜和细胞质膜合并？它们有区别吗？",
    "context": {
      "recent_integration": {
        "pairs": [...],
        "stats": {...}
      }
    }
  }'
```

响应示例：
```json
{
  "reply": "这两个术语确实指的是同一结构，只是命名习惯不同。在您的教材中，第一本使用'细胞膜'，第二本使用'细胞质膜'。从生物学定义来看，它们完全等价，都是指包围细胞质的磷脂双分子层结构。合并后保留了更详细的定义描述，同时补充了第二本教材中的'边界'概念，使定义更完整。",
  "actions": [
    {
      "type": "UPDATE_KP",
      "target_id": "kp_001",
      "new_content": "细胞膜（又称细胞质膜）是包围细胞质的薄膜，由磷脂双分子层和蛋白质组成，是细胞与外界环境的边界"
    }
  ]
}
```

### 竞学堂

#### GET /api/arena/opponents — 获取对手列表

```bash
curl http://localhost:8000/api/arena/opponents
```

响应示例：
```json
[
  {
    "id": "wishdel",
    "name": "维什戴尔",
    "personality": "自信张扬、争强好胜",
    "style": "简洁犀利、喜欢用反问句和挑衅语气",
    "emoji": "⚔️",
    "color": "red"
  },
  {
    "id": "teresia",
    "name": "特雷西娅",
    "personality": "沉稳冷静、理性分析",
    "style": "温和但暗含讽刺、用优雅方式嘲笑对手",
    "emoji": "🗡️",
    "color": "purple"
  }
]
```

#### POST /api/arena/start — 开始对局

```bash
curl -X POST http://localhost:8000/api/arena/start \
  -H "Content-Type: application/json" \
  -d '{"mode": "student"}'
```

响应示例：
```json
{
  "session_id": "arena_001",
  "mode": "student",
  "total_rounds": 8,
  "opponents": ["wishdel", "teresia"],
  "status": "ready"
}
```

#### POST /api/arena/answer — 提交答案

```bash
curl -X POST http://localhost:8000/api/arena/answer \
  -H "Content-Type: application/json" \
  -d '{
    "session_id": "arena_001",
    "round": 1,
    "user_answer": "A"
  }'
```

响应示例：
```json
{
  "round": 1,
  "question": {
    "text": "以下关于细胞膜的描述，正确的是？",
    "options": {
      "A": "细胞膜由单层磷脂分子组成",
      "B": "细胞膜由双层磷脂分子和蛋白质组成",
      "C": "细胞膜只含有蛋白质",
      "D": "细胞膜只含有磷脂"
    },
    "correct_answer": "B",
    "explanation": "细胞膜的流动镶嵌模型指出，细胞膜由磷脂双分子层构成基本骨架，蛋白质分子镶嵌其中或贯穿膜层。",
    "source": {
      "textbook": "test.pdf",
      "chapter": "第一章 细胞结构",
      "page": 15
    }
  },
  "user_answer": "A",
  "is_correct": false,
  "score_change": 0,
  "streak_reset": true,
  "opponent_answers": {
    "wishdel": {
      "answer": "B",
      "is_correct": true,
      "taunt": "哈！连这么基础的题都不会？细胞膜是双层结构，这是常识啊！"
    },
    "teresia": {
      "answer": "B",
      "is_correct": true,
      "taunt": "也许你需要回去复习一下基础知识呢，别着急，慢慢来。"
    }
  },
  "explanation": "细胞膜的流动镶嵌模型指出，细胞膜由磷脂双分子层构成基本骨架，蛋白质分子镶嵌其中或贯穿膜层。单层磷脂分子无法形成稳定的膜结构。",
  "next_round_available": true
}

## 5. RAG流程

```
用户提问
   ↓
Embedding (BGE-small-zh)
   ↓
FAISS Top-5 检索
   ↓
拼接上下文 + 引用来源
   ↓
LLM生成回答
   ↓
返回answer + references[]
```

## 6. 知识整合流程

```
所有知识点
   ↓
快速相似度筛选 (字符集Jaccard)
   ↓
Top-20 相似对
   ↓
LLM逐一判断: merge/keep/remove
   ↓
生成 IntegrationResult
   ↓
教师审阅 → 自然语言修改
   ↓
应用到图谱
```
